
import logoImage from './logo.png';

export const Logo = ({ className }: { className?: string }) => {
  const scrollToTop = () => {
    window.scrollTo({
      top: 0,
      behavior: "smooth"
    });
  };

  return null;
};
